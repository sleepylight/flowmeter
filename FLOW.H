#define debug 0

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

double WaterToPsi(double x);   /*converts inches of H2O to PSI*/
double MmhgToPsi(double x);   /*converts mm of mercury to PSI*/
double CelToFah(double x);   /* converts degrees C to degrees F */
double KelToFah(double x);   /* converts degrees K to degrees F */
double CfmToLpm(double x); /* Cubic feet per min to Litres per min*/
double MmToCfm(double x); /* Uses an equation generated by Sigma plot that */
   /* approximates the curve given to us by manufaturer to covert a reading in */
   /* mm on the rotometer to a flowrate in Cubic Feet per Minute */
double getpress(void);   /* get the ambient pressure if needed */
double getrotoheight(void);  /* get the float height of the rotometer */
double gettemp(void);  /* get the ambient temprature */
double getpressdrop(void); /* get the drop in pressure */
double correction(double press, double pressdrop, double temp, double rotometer);
double curve1(void); /* fits rotometer height to an equatoin and converts to LPM */
double curve2(void); /* fits rotometer height to an equation and converts to LPM */
double nocurve(void); /* gets input from user and converts to LPM */

